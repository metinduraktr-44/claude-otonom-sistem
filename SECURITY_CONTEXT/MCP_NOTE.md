# SECURITY_CONTEXT note

Security MCP servers are documented in `.cursor/mcp.security.example.json` (Semgrep/Snyk placeholders with `${VAR}`).

**Default: OFF.** Do not merge into live `.cursor/mcp.json` unless user enables.

Agency Canva MCP (other branch) must not be removed when merging.
