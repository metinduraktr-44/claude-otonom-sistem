---
description: Secret hygiene — asla gerçek secret yazma
globs: **/*
---

# Secret Hygiene

Gerçek veya realistic secret **YASAK**. Kullan: `${VAR}`, `vault://path`, `op://item`, `<REDACTED>`.

`.env` commit etme (gitignore). Örnek: `.env.example` boş değer. Bulgu: konum + tip; değer loglama.

Referans: `docs/SECRETS-DRYRUN-MATRISI.md`. Hook: `scripts/secret_scan.py`.
