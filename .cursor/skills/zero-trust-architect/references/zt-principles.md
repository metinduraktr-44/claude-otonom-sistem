# Zero trust principles (Holding uyarlaması)

1. Never trust agent output — ethics + secret gates
2. Explicit MODE for mutate
3. Segment katalog vs runtime
4. Continuous validate (CI)
5. Assume breach → IR playbook skeleton
6. Least privilege identities (IAM)

Mapping: NIST CSF Protect/Detect · CTRL-L-* · CONDITIONAL

> DEFENSE-ONLY: detection, hardening, compliance, IR playbooks. No exploits, phishing, malware, C2, ransomware, bypass, weaponized PoCs. ATT&CK only for detection/D3FEND mapping. Secrets: ${VAR}|vault://|op://|<REDACTED> only.
