---
name: canva-bulk-create
description: Bulk-create variants from brief matrix (Enterprise autofill when gated)
---

# canva-bulk-create

From `BRIEFS/` + dataset CSV → autofill jobs (Enterprise). Poll 2–5s.
Without Enterprise: generate N brief variants under `SCENARIOS/{urun}/{n}/` only.

